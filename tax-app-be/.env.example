PORT=3001
DATABASE_URL=postgres://coretax:coretax_dev_pw@localhost:5433/coretax
SESSION_COOKIE=coretax_session
CORS_ORIGIN=http://localhost:3000

# Hackathon demo only: lets the login page's sign_in_demo WebMCP tool open a
# session for the synthetic demo taxpayer without any credential. Set to
# `true` only on the demonstration deployment or a local demo run; it enables
# passwordless sign-in for the synthetic account.
DEMO_LOGIN_ENABLED=false
DEMO_LOGIN_USERNAME=0912345678901234
